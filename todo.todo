BUG since rework:

[ ] cant edit a message properly now (used editing in place, inside box, but now no more boxes)
[ ] Lets add a mode with explicit role prefix for each message, like "pico: " or "user: " (optional, configurable)

