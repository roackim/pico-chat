[ ] add deepseek provider
[x] fix input box problem where no max height, and it takes whole heigh if paste long content, and no scroll
[x] fix focus message error
[x] redo /model to a modal list selector/
[x] fix reading model context window from server (shows 32k for deepseek despite being like 1M)

[x] copy to clipboard from ssh
    - uses xclip (require ssh -X)
    
    
[x] interpolate streaming for much smoother experience 
    [x] based on actual throughput -> start very fine grained interp, if we too slow for streaming, catch up with coarser grainsI 
    
[x] better markdown interpreting (bold sucks)/con
[x] support multiple styles / themes definitions with command for selection like /model
    [x] have a themes.json similar to servers ? 
[x] remove /pwd /cd /openrouter(?) /roles /tools /servers /debug(?) /activity /status(?)

[x] remove system prompt, only roles set their own system prompt
[x] /config role <SHOW THE FUCKING selector suggestion etc>

[ ] bug in / command selector with up and down and scrolls (weirdness) works but non ergonomic     

[ ] SKILLS ?
[ ] AGENTS.md AGENT.md (ROBOT.md ROBOT.txt (?))


[ ] add [r] raw copy (non interpreted)
[ ] finer grain for copy, like code block, table etc
    [ ] implies finer grain for focus ? MOD key + up / down / click ?
        -> focus part of message ?
        
        
[x] update tools definition
[ ] tables columns are getting cut despite horizontal space available 