
[x] Msg Rendering
    [x] pre render / format thoughts too (AT LEAST \n)
        - Normalised in _format_line_wrap: trailing whitespace stripped, consecutive blank lines collapsed
        - append() strips leading whitespace from every chunk
    [x] Code render
        - respect indentation (hard-break wrapping for code blocks, preserves leading spaces)
        - apply basic syntax highlighting
            - keyword lists for 15+ languages (Python, JS, TS, Lua, Rust, C, C++, Bash, SQL, Go, Java, Ruby, PHP, TOML, YAML, JSON)
            - function call detection (identifier followed by parenthesis)
            - also highlights: strings, comments, numbers
    [x] Agent messages are missing the single whitespace padding on the left
        - MarkdownComponent now accepts left_pad parameter, applied in render/wrap
        - Wired through Message constructor
    [x] When the model makes a tool call right after a message, the message is incomplete
        - current_msg.finalize() called before transitioning to ToolDraft or ToolStatusChange
        - Ensures the text message is flushed and rendered completely before the tool message appears
    [x] render properly \n\n and \n from thinking messages (interpret as new line)
    [x] markdown title / subsections etc have spaces stripped out
    [x] render tables properly with AsciiTable vendored module
    [x] when a Pico Msg is finished, do not display empty trailing lines

[x] Proper commands / parameters hints schemas

[ ] When editing in a message in place, allow for the fuzzy @ to work too

[ ] allow to delete a queued message by simply removing from the queue instead of regenerating the convo
[ ] deleting a non convo message should not stop generation

[x] for permission tool:
    - change prefix from > to ? while asking, make colors more visible too like purple or magenta
    
[ ] improve the output action
    - make it reversible, so that if you click it again, it will undo the action
    - tools: avoid duplicating the cmd in the output 


[x] Better mouse support
    [x] Support mouse selection inside a message    
        - drag to select, reverse-video highlight, auto-copy on release
        - 'y' key yanks (copies) the current selection
    [x] make actions clickable like "[c] copy" 
        - action bar hit-tested on-demand in ChatHistoryPanel (works pre-focus)

[ ] New UI
    [ ] Popups
        [x] escape key closes popup (action)
        [x] /help moved to popup
        [x] /status moved to popup (async with placeholder)
        [x] /tools moved to popup
        [x] /permissions moved to popup
        [x] /debug help moved to popup
        [ ] Move debug panel to a separate conversation
        
    [ ] Forms
        [ ] Toggle for settings
        [ ] Open fields
        [ ] Checkboxes list   [ ] | [x]
        [ ] Radio list        ( ) | (x)
        
    [ ] Add a status line on top of input field (instead of relying on chat messages)
        [ ] Copied to clipboard ! (with timeout ? / overwrite)
        [ ] Add an indicator for down message like "+3 messages"
    

[ ] Containers
    [ ] Move from bubblewrap to podman
        cf plans/proper_container_integration.md
        
[ ] Subagent: kill / abort  action
    [?] Focus ? 
        popup 

[ ] Conversation Management
    [ ] Export conversation to file
    [ ] Import conversation from file
        /conversation import|export <filename> 
    [ ] Multiple conversations (tabs)
    
[ ] Remove token count estimations, rely on server instead / when possible ?

[ ] Dynamic reasoning budget ?

[ ] Git integration
    /git branch new - create a new branch for the current conversation
    /git branch list - list all pico branches as tree
    /git commit changes - commit all the changes
        - keep list of all modified files
        - keep list of all created files
        - prompt user with a FORM popup to
            - enter commit message
            - select files to commit (checkboxes) (checked by default)
            
    /git squash - squash all the changes into a single commit into base branch
    /git rebase - rebase the current branch onto base branch
    

[ ] How to handle servers that serves multiple models ? 
    -> like ollama
    